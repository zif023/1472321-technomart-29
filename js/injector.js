// Elements to inject
var mySVGsToInject = document.querySelectorAll('img.inject-me');
     
// Do the injection
SVGInjector(mySVGsToInject);
//$('.new-product').each(function(i, div){$(div).html($(div).html()+"<h1>NEW</h1>")})